/*global require,module,__dirname*/
const path = require('path')
module.exports = {
  entry: './temp/import/main.js',
  output: {
    filename: 'main.min.js',
    path: path.resolve(__dirname, 'temp/import')
  },
  mode: 'production',
  devtool: 'source-map',
  module: {
    rules: [
      {
        test: /\.js$/,
        loader: 'babel-loader',
        exclude: /node_modules/,
        query: {
          presets: ['es2015', 'stage-0', 'minify']
        }
      }
    ]
  }
}